jsonp调用回调函数时，是调用的window.callback。


 相对JSONP，CORS支持POST提交



proxy代理以及nginx反向代理


-----

Spring Data JPA 是 Spring 基于 ORM 框架、JPA 规范的基础上封装的一套JPA应用框架


@NoRepositoryBean


classpath：只会到你的class路径中查找找文件。

classpath*：不仅包含class路径，还包括jar文件中（class路径）进行查找。  //避免使用


伪静态  urlrewritefilter



-----------

es5-shim可以让一些低级的浏览器支持最新的ecmascript5的一些特性。

Polyfill是一个js库,主要抚平不同浏览器之间对js实现的差异。



响应式流（Reactive Stream）
Reactor支持响应式流规范，与RxJava相比，它没有任何历史包袱，专注于Server端的响应式开发，而RxJava更多倾向于Android端的响应式开发。
在Java 9版本中，响应式流的规范被纳入到了JDK中，相应的API接口是java.util.concurrent.Flow。

Undertow

“Backpressure”（背压）  “Flow Control”（流控）的一种方案，其他两个：节流（Throttling），打包（buffer和window）

Apache的ab命令模拟多线程并发请求

--0731


erwin


sitemesh


vue.js

eureka springcloud服务治理

swagger
http://10.11.2.172:8087/policy/getPolicyNewsList?policyType=0&subPolicyType=0&areaType=0&policySort=0&orderby=time&page=1&rows=10


-------------

 MIT License (MIT)


------

App通常用restful api跟server打交道。Rest是stateless的，也就是app不需要像browser那样用cookie来保存session, 因此用session token来标示自己就够了，session/state由api server的逻辑处理。


----

0810 完成大部分企业动态功能开发

----
0813 优化excel下载。加入新能源。奖励类型

（剩下激励查询比例功能）

----
0814  重新计算mix，msrp，tp

------

一般而言，应该避免直接PO传递到系统中的其他层面，一种解决办法是，通过一个VO，通过属性复制使其具备与PO相同属性值，并以其为传输媒质（实际上，这个VO被用作Data Transfer Object，即所谓的DTO），将此VO传递给其他层面以实现必须的数据传送。

----

817
ResultSetHandler

-----
820
isPrimitive

成员变量：在类体的变量部分中定义的变量，也称为属性。

JDBC不再需要Class.forName()来显式加载jdbc驱动

需要注意的是，如果之前有一个与当前字段id相同的字段，在同一条记录中对同一个对象做了set操作，那么当前这个id字段将不会再对当前对象做set操作。

ObjectWrapper   CollectionWrapper 

PropertyTokenizer

高级关联和集合映射

迭代子模式可以顺序地访问一个聚集中的元素而不必暴露聚集的内部表象（internal representation）


hibernate并没有提供相应的二级缓存的组件，所以需要加入额外的二级缓存包，常用的二级缓存包是EHcache。
Hibernate的二级缓存策略，是针对于ID查询的缓存策略，对于条件查询则毫无作用。为此，Hibernate提供了针对条件查询的Query Cache。

------

821

select t.salarymonth,
       sum(t.salary) / (select sum(t1.salary)
                          from D_MONTH_SALARY t1
                         where t1.salarymonth = (substr(t.salarymonth, 1, 4) - 1) ||
                               substr(t.salarymonth, -2)) "同比",
       sum(t.salary) /
       (select sum(t1.salary)
          from D_MONTH_SALARY t1
         where t1.salarymonth =
               to_char(to_date(t.salarymonth, 'yyyymm') - 1, 'yyyymm')) "环比"
  from D_MONTH_SALARY t
 group by t.salarymonth;

限流的常用处理手段有：计数器、滑动窗口、漏桶、令牌。
Guava RateLimiter基于令牌桶算法

-----
822
6 大设计原则，合称 SOLID
BFF全称是Backends For Frontends(服务于前端的后端)

----

消息中间件规范JMS 2.0引入了一个新对象——JMSContext


webpack  nodejs打包模块


跨域解决方案

1、 通过jsonp跨域
2、 document.domain + iframe跨域
3、 location.hash + iframe
4、 window.name + iframe跨域
5、 postMessage跨域
6、 跨域资源共享（CORS）
7、 nginx代理跨域
8、 nodejs中间件代理跨域
9、 WebSocket协议跨域



 jsoup 是一款Java 的HTML解析器   爬虫

CI系统 企业形象

JPA是出自EJB3

----
824
WARO(Write All Read one)
Quorum机制
QJM机制(Quorum Journal Manager)实现HDFS的HA（High Availability）  数据同步就用到了Quorum机制
Raft算法(zookeeper核心算法)
etcd
服务品质协议(service-level agreement)(SLA)
PWA(Progressive Web App)
v8 引擎的设计限制，在 32-bit下有 1GB 最大堆尺寸的限制，在 64-bit下是1.7GB。当然，由于 node.js buffer 的分配不是在 v8 的堆上，因此可以超过此限制。
胶水语言

--
828
修复一个数据bug  left join

--
830
on是最先执行，where次之，having最后。
inner join中on和where没区别，右连接和左连接不一样

--
831
JS 解析器 rhino(Java)
 Avatar.js 是依赖于Nashorn用以支持在JVM上实现Node.js编程模型

---
 0903
 @Around
环绕增强，相当于MethodInterceptor

---
0904
hash join是一种数据库在进行多表连接时的处理算法，对于多表连接还有两种比较常用的方式：sort merge-join 和 nested loop

@inject = @autowired
@javax.inject.Named和Spring中的@Component的职责类似

开闭原则
--
95
Buffer是缓冲区
Cache 是高速缓存
数据都是有高速缓存得到的
linux的设计减少一次次缺页处理。
$('#laneconfigdisplay').trigger('contentchanged');


--
913
@CrossOrigin(methods = { RequestMethod.GET, RequestMethod.POST}, origins = "*")//for test

--
917
但如果SELECT子句中出现了DISTINCT关键字，则只能用出现过的列名，


----
921

function formatNum(strNum) {
                if(strNum.length <= 3) {
                    return strNum;
                }
                if(!/^(\+|-)?(\d+)(\.\d+)?$/.test(strNum)) {
                    return strNum;
                }
                var a = RegExp.$1,
                    b = RegExp.$2,
                    c = RegExp.$3;
                var re = new RegExp();
                re.compile("(\\d)(\\d{3})(,|$)");
                while(re.test(b)) {
                    b = b.replace(re, "$1,$2$3");
                }
                return a + "" + b + "" + c;
            }

			
			  formatter: function(p) {
        return p.name+":"+formatNum(p.value)+"万元";
                            }


----
926
粘包
Kylin是ebay开发的一套OLAP系统，与Mondrian不同的是，它是一个MOLAP系统

--
1018项目预发布了 耗时挺久的

@autofe/carpicker  汽车之家车型选择控件

---
1020
public static boolean isNumeric(String str){
		// 就是判断是否为整数(正负)
		Pattern pattern = 
			Pattern.compile("^\\d+$|-\\d+$"); 
		//判断是否为小数(正负)
		Pattern pattern2 = 
			Pattern.compile("\\d+\\.\\d+$|-\\d+\\.\\d+$");
		return (pattern.matcher(str).matches() 
			|| pattern2.matcher(str).matches());
	}


---

1022
list.subList()方法引起的异常
java.util.ConcurrentModificationException


----
1029

int randNumber =rand.nextInt(MAX - MIN + 1) + MIN; // randNumber 将被赋值为一个 MIN 和 MAX 范围内的随机数

终于上了个算完善的版本
---
1030 
在浏览器中，DOM的实现和ECMAScript的实现是分离的。比如 在IE中，ECMAScrit的实现在jscript.dll中，而DOM的实现在mshtml.dll中；
在Chrome中使用WebKit中的 WebCore处理DOM和渲染，但ECMAScript是在V8引擎中实现的，其他浏览器的情况类似。
所以通过JavaScript代码调用DOM接 口，相当于两个独立模块的交互。

DOM操作对性能影响最大其实还是因为它导致了浏览器 的重绘（repaint）和回流（reflow）。


diff算法  传统diff  和 react diff算法

---
1101
SSDB 是一个高性能 NoSQL 数据库, 使用 LevelDB 作为存储引擎

灰度发布

---
1114
sync.acquire(1)

--
1115
Axios 是一个基于 promise 的 HTTP 库,可以用在浏览器和 node.js 中


BIDUPSID  浏览器中  保存百度搜索记录

expire_in  多长时间过期

---

1116
缓存一致性协议

Java内存模型的主要目标是定义程序中各个变量的访问规则
MSI、MESI、MOSI及Dragon Protocol


https://www.eclipse.org/downloads/packages/release


----
1119
java.lang.OutOfMemoryError：GC overhead limit exceeded

GC overhead limt exceed检查是Hotspot VM 1.6定义的一个策略，通过统计GC时间来预测是否要OOM了，提前抛出异常，防止OOM发生。
Sun 官方对此的定义是：“并行/并发回收器在GC回收时间过长时会抛出OutOfMemroyError。
过长的定义是，超过98%的时间用来做GC并且回收了不到2%的堆内存。用来避免内存过小造成应用不能正常工作。”




纵向分表  横向分表

冷数据  
对热数据，可以相对有更多的主库的横向分表处理。 



重定向可以解决表单重复提交的问题

--

1120
分区的主要目的还是避免了全表扫描
亿级数据多条件组合查询使用HBase+Solr的解决方案能够满足秒级响应的需求

HAWQ

---
1121
spring-boot-starter-security

直接配置URL路由的方式，用不上Ribbon，也用不上Hystrix，Zuul转发所使用的过滤器是 


----
1123
@Async

Spring事务是通过ThreadLocal类来实现,所以只有主线程有事务管理.

---
1126
HMAC是IP安全里必须实现的MAC方案，并且其他Internet协议中（如SSL）也使用了HMAC。HMAC已作为NIST标准发布。

 锁的实现 Synchronized是依赖于JVM实现的,而ReenTrantLock是JDK实现。

happen before原则  对volatile字段的写入操作先于读操作


----
1203

从朋友圈到公众号、从微信支付到小程序，算法在微信的版图里一直处于边缘的状态，即便是严重依赖算法的广告，
微信最先推出的也是对算法要求没那么高的品牌广告而非效果广告


基于用户的推荐逻辑其算法复杂程度远远高于基于内容的推荐

C端产品更看重的是用户体验，B端的更看重业务如何实现  多数是C端
后端产品经理的首要目标就是理解业务的全貌，能够让业务正确的运转并对效果进行评估


jtds


jfinal       JfinalUIB


jetty部署异常   org.apache.jasper.JasperException: PWC6345: There is an error in invoking javac.  A full JDK (not just JRE) is required



-----------
1205
实现响应式布局的核心是栅格系统   与bootstrap一时瑜亮的Foundation

Webx 3.0和Spring MVC一样，完全建立在Spring框架之上

----
1217

@Before(Tx.class)写在controller中会回滚

----
1221
稀疏数据是指，数据框中绝大多数数值缺失或者为零的数据

----
1224
